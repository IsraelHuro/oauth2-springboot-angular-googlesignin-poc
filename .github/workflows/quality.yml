name: quality

on:
  pull_request:
    branches: [ "develop" ]
  push:
    branches: [ "**" ]

jobs:
  backend-authorization:
    name: authorization-server (checkstyle + tests)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: authorization-server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Cache Maven repo
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml', '.mvn/wrapper/maven-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Prepare Maven Wrapper
        run: |
          sed -i 's/\r$//' mvnw
          chmod +x mvnw

      - name: Maven test (retry + full errors)
        run: |
          set -e
          for i in 1 2 3; do
            echo "Attempt $i/3"
            ./mvnw -B -ntp -e -DtrimStackTrace=false test && break
            if [ "$i" -eq 3 ]; then
              echo "Maven failed after 3 attempts"
              exit 1
            fi
            echo "Retrying in 15s..."
            sleep 15
          done

  backend-resource:
    name: resource-server (checkstyle + tests)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: resource-server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Cache Maven repo
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml', '.mvn/wrapper/maven-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Prepare Maven Wrapper
        run: |
          sed -i 's/\r$//' mvnw
          chmod +x mvnw

      - name: Maven test (retry + full errors)
        run: |
          set -e
          for i in 1 2 3; do
            echo "Attempt $i/3"
            ./mvnw -B -ntp -e -DtrimStackTrace=false test && break
            if [ "$i" -eq 3 ]; then
              echo "Maven failed after 3 attempts"
              exit 1
            fi
            echo "Retrying in 15s..."
            sleep 15
          done

      - name: Dump Surefire reports (on failure)
        if: failure()
        run: |
          echo "==== SUREFIRE REPORTS (resource-server) ===="
          ls -la target/surefire-reports || true
          for f in target/surefire-reports/*.txt target/surefire-reports/*.dumpstream target/surefire-reports/*.xml; do
            if [ -f "$f" ]; then
              echo "---- $f ----"
              sed -n '1,200p' "$f"
            fi
          done

  frontend-lint:
    name: ag-prueba-front (eslint)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ag-prueba-front
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: ag-prueba-front/package-lock.json

      - name: Install deps
        run: npm ci --no-audit --no-fund

      - name: Lint
        run: npm run lint
